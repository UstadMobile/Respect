# lib-xapi-test

This module contains reusable tests that verify Experience API (xAPI) functionality e.g. to store
and retrieve statements, documents, etc. This module can be used as a dependency in unit tests for
all datalayers because the expected results are the same regardless of implementation (e.g. 
local database, http client/server, IPC client/server, etc).

