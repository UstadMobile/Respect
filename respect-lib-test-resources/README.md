# respect-lib-test-resources

Contains sample xAPI statements (in future maybe other resources) for use with tests (such that they 
can be added as a dependency instead of being copied to multiple test resource directories).
